The scripts here should be added to your crontab. Mine is:

0,15,30,45 * * * * ~/s/cron/update_local_repositories.sh >/dev/null 2>&1

To edit your crontab file:

$ crontab -e

To see your current crontab:

$ crontab -l
